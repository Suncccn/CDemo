#include <stdio.h>
#include <netdb.h>
#include <sys/socket.h>
#include <sys/utsname.h>
#include <errno.h>
#include <string.h>
#include <sys/types.h>


int main(int argc, char *argv[])
{
	// struct utsname uts;
	// uname(&uts);
	// printf("%s\n%s\n", uts.sysname,uts.nodename);
	// struct in_addr addr;
	// printf("%s\n", strerror(errno));
	
	return 0;
}